# Local state of the bootstrap stack: it holds the state bucket's identity and must
# never be committed. Recreating the bucket by hand is a five-minute job — see
# bootstrap/README.md.
bootstrap/.terraform/
bootstrap/terraform.tfstate*
**/.terraform/
*.tfvars
!*.tfvars.example
.DS_Store
